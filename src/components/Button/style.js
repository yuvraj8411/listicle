import { colors } from "../../utils/color";

export const styles = {
    container: {
        paddingVertical: 16,
        paddingHorizontal: 8,
        backgroundColor: colors.blue,
        borderRadius: 8,
        width: '100%',

    },
    buttonText: {
        color: colors.white,
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold',
    },
}